<%= form_for(benefit) do |f| %>
  <% if benefit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(benefit.errors.count, "error") %> prohibited this benefit from being saved:</h2>

      <ul>
      <% benefit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_area :nombre %>
  </div>

  <div class="field">
    <%= f.label :areabenefit_id %>
    <%= f.collection_select(:areabenefit_id, Areabenefit.all, :id, :nombre) %>
  </div>

  <div class="field">
    <%= f.label :costotrabajador %>
    <%= f.number_field :costotrabajador %>
  </div>

  <div class="field">
    <%= f.label :costoempresa %>
    <%= f.number_field :costoempresa %>
  </div>

  <div class="field">
    <%= f.label :familia %>
    <%= f.number_field :familia %>
  </div>

  <div class="field">
    <%= f.label :asistencia %>
    <%= f.number_field :asistencia %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
