<h1>Hola MUndo</h1>

<h2> Log Intermedio </h2>

<%=form_for Benefit.new, url: {controller: "pages", action: "indexBenefitPerName"} do %>

	<div class="field">
	<%= collection_select(:benefit, :benefit_id, Benefit.all, :id, :nombre, {:prompt => "---Seleccionar Beneficio---"}, {:class => "contact_selection"}) %>
	</div>

    <div class="field">
    <%= collection_select(:centrocosto, :centrocosto_id, Centrocosto.all, :id, :codigo, {:prompt => "Escoger Obra"},{:class => "ccosto_selection"}) %>
    </div>


	<div class="actions">
	<%= submit_tag 'Create' %>
	</div>
<% end %>

<h2> Log Final de Asistencia </h2>


<%=form_for Benefit.new, url: {controller: "pages", action: "logBeneficiosCheckAsistencia"} do %>

	<div class="field">
	<%= collection_select(:benefit, :benefit_id, Benefit.all, :id, :nombre, {:prompt => "---Seleccionar Beneficio---"}, {:class => "contact_selection"}) %>
  	</div>


  	<div class="field">
    <%= collection_select(:centrocosto, :centrocosto_id, Centrocosto.all, :id, :codigo, {:prompt => "Escoger Obra"},{:class => "ccosto_selection"}) %>
    </div>

  	<div class="actions">
    <%= submit_tag 'Create' %>
  	</div>
<% end %>
